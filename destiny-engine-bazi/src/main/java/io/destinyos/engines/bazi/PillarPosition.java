package io.destinyos.engines.bazi;

/** Which of the Tứ Trụ (four pillars) a {@link BaziPillar} is. */
public enum PillarPosition {
    /** Trụ Năm. */
    YEAR,
    /** Trụ Tháng. */
    MONTH,
    /** Trụ Ngày — its stem is the Nhật Chủ (Day Master). */
    DAY,
    /** Trụ Giờ. */
    HOUR
}
