<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>io.destinyos</groupId>
  <artifactId>destiny-os</artifactId>
  <version>0.1.0-SNAPSHOT</version>
  <packaging>pom</packaging>

  <name>Destiny OS</name>
  <description>
    Deterministic Metaphysical Computation and Scenario Decision-Support Platform.
    Phase 1: project foundation. Contains NO metaphysical calculation.
  </description>

  <!--
    Modular monolith (ADR D1). Microservices are explicitly rejected.
    Module order below reflects the dependency direction:
    core -> engine-api -> execution, with i18n and app on top.
  -->
  <modules>
    <module>destiny-core</module>
    <module>destiny-calendar</module>
    <module>destiny-engine-api</module>
    <module>destiny-execution</module>
    <module>destiny-i18n</module>
    <module>destiny-persistence</module>
    <module>destiny-engine-tarot</module>
    <module>destiny-engine-numerology</module>
    <module>destiny-engine-bazi</module>
    <module>destiny-engine-fengshui</module>
    <module>destiny-engine-astrology</module>
    <module>destiny-fusion</module>
    <module>destiny-scenario</module>
    <module>destiny-ai</module>
    <module>destiny-api</module>
    <module>destiny-app</module>
  </modules>

  <properties>
    <maven.compiler.release>21</maven.compiler.release>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>

    <spring-boot.version>3.3.5</spring-boot.version>
    <archunit.version>1.3.0</archunit.version>
    <assertj.version>3.26.3</assertj.version>
    <junit.version>5.10.5</junit.version>
    <awaitility.version>4.2.2</awaitility.version>
  </properties>

  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-dependencies</artifactId>
        <version>${spring-boot.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>

      <dependency>
        <groupId>com.tngtech.archunit</groupId>
        <artifactId>archunit-junit5</artifactId>
        <version>${archunit.version}</version>
        <scope>test</scope>
      </dependency>

      <dependency>
        <groupId>org.assertj</groupId>
        <artifactId>assertj-core</artifactId>
        <version>${assertj.version}</version>
        <scope>test</scope>
      </dependency>

      <dependency>
        <groupId>org.awaitility</groupId>
        <artifactId>awaitility</artifactId>
        <version>${awaitility.version}</version>
        <scope>test</scope>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <dependencies>
    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.assertj</groupId>
      <artifactId>assertj-core</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <pluginManagement>
      <plugins>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-compiler-plugin</artifactId>
          <version>3.13.0</version>
          <configuration>
            <compilerArgs>
              <arg>-parameters</arg>
              <arg>-Xlint:all</arg>
            </compilerArgs>
          </configuration>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-surefire-plugin</artifactId>
          <version>3.5.2</version>
        </plugin>
      </plugins>
    </pluginManagement>
  </build>
</project>
